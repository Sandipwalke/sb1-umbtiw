import React from 'react';

const JsonToCsv: React.FC = () => {
  return (
    <div>
      <h1>JSON to CSV Converter</h1>
      <p>This tool allows you to convert JSON data into CSV format.</p>
      {/* Add the actual JSON to CSV conversion logic and UI here */}
    </div>
  );
};

export default JsonToCsv;