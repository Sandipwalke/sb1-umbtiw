import React from 'react';

const MemeGenerator: React.FC = () => {
  return (
    <div>
      <h1>Meme Generator</h1>
      <p>This tool allows you to create custom memes by adding text to images.</p>
      {/* Add the actual meme generation logic and UI here */}
    </div>
  );
};

export default MemeGenerator;