import React from 'react';

const HtmlToMarkdown: React.FC = () => {
  return (
    <div>
      <h1>HTML to Markdown</h1>
      <p>This tool allows you to convert HTML code into Markdown format.</p>
      {/* Add the actual HTML to Markdown conversion logic and UI here */}
    </div>
  );
};

export default HtmlToMarkdown;