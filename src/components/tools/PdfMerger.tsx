import React from 'react';

const PdfMerger: React.FC = () => {
  return (
    <div>
      <h1>PDF Merger</h1>
      <p>This tool allows you to combine multiple PDF files into a single PDF document.</p>
      {/* Add the actual merging logic and UI here */}
    </div>
  );
};

export default PdfMerger;