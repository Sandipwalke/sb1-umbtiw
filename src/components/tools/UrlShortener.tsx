import React from 'react';

const UrlShortener: React.FC = () => {
  return (
    <div>
      <h1>URL Shortener</h1>
      <p>This tool allows you to create short, easy-to-share links from long URLs.</p>
      {/* Add the actual URL shortening logic and UI here */}
    </div>
  );
};

export default UrlShortener;