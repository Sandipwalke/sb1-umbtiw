import React from 'react';

const ResizeImage: React.FC = () => {
  return (
    <div>
      <h1>Resize Image</h1>
      <p>This tool allows you to resize images to your desired dimensions.</p>
      {/* Add the actual image resizing logic and UI here */}
    </div>
  );
};

export default ResizeImage;