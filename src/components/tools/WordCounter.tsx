import React from 'react';

const WordCounter: React.FC = () => {
  return (
    <div>
      <h1>Word Counter</h1>
      <p>This tool allows you to count the number of words in a given text.</p>
      {/* Add the actual word counting logic and UI here */}
    </div>
  );
};

export default WordCounter;