import React from 'react';

const ImageToText: React.FC = () => {
  return (
    <div>
      <h1>Image to Text (OCR)</h1>
      <p>This tool allows you to extract text from images using Optical Character Recognition (OCR) technology.</p>
      {/* Add the actual OCR logic and UI here */}
    </div>
  );
};

export default ImageToText;