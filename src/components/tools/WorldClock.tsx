import React from 'react';

const WorldClock: React.FC = () => {
  return (
    <div>
      <h1>World Clock</h1>
      <p>This tool displays the current time in various time zones around the world.</p>
      {/* Add the actual world clock logic and UI here */}
    </div>
  );
};

export default WorldClock;