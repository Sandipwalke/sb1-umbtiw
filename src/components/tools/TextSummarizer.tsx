import React from 'react';

const TextSummarizer: React.FC = () => {
  return (
    <div>
      <h1>Text Summarizer</h1>
      <p>This tool automatically generates a concise summary of longer texts.</p>
      {/* Add the actual text summarization logic and UI here */}
    </div>
  );
};

export default TextSummarizer;