import React from 'react';

const QrCodeGenerator: React.FC = () => {
  return (
    <div>
      <h1>QR Code Generator</h1>
      <p>This tool allows you to generate QR codes from text or URLs.</p>
      {/* Add the actual QR code generation logic and UI here */}
    </div>
  );
};

export default QrCodeGenerator;