import React from 'react';

const Stopwatch: React.FC = () => {
  return (
    <div>
      <h1>Stopwatch</h1>
      <p>This tool provides a stopwatch for timing events or activities.</p>
      {/* Add the actual stopwatch logic and UI here */}
    </div>
  );
};

export default Stopwatch;