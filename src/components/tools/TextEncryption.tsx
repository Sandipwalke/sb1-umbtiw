import React from 'react';

const TextEncryption: React.FC = () => {
  return (
    <div>
      <h1>Text Encryption (AES-256)</h1>
      <p>This tool allows you to encrypt and decrypt text using the AES-256 encryption algorithm.</p>
      {/* Add the actual text encryption/decryption logic and UI here */}
    </div>
  );
};

export default TextEncryption;