import React from 'react';

const RotateImage: React.FC = () => {
  return (
    <div>
      <h1>Rotate Image</h1>
      <p>This tool allows you to rotate images to your desired angle.</p>
      {/* Add the actual image rotation logic and UI here */}
    </div>
  );
};

export default RotateImage;