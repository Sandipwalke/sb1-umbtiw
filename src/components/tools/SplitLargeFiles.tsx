import React from 'react';

const SplitLargeFiles: React.FC = () => {
  return (
    <div>
      <h1>Split Large Files</h1>
      <p>This tool allows you to split large files into smaller, more manageable parts.</p>
      {/* Add the actual file splitting logic and UI here */}
    </div>
  );
};

export default SplitLargeFiles;