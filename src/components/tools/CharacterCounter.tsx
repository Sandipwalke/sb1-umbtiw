import React from 'react';

const CharacterCounter: React.FC = () => {
  return (
    <div>
      <h1>Character Counter</h1>
      <p>This tool allows you to count the number of characters in a given text.</p>
      {/* Add the actual character counting logic and UI here */}
    </div>
  );
};

export default CharacterCounter;