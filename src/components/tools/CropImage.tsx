import React from 'react';

const CropImage: React.FC = () => {
  return (
    <div>
      <h1>Crop Image</h1>
      <p>This tool allows you to crop images to your desired dimensions.</p>
      {/* Add the actual image cropping logic and UI here */}
    </div>
  );
};

export default CropImage;