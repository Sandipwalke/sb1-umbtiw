import React from 'react';

const WordToPdf: React.FC = () => {
  return (
    <div>
      <h1>Word to PDF Converter</h1>
      <p>This tool allows you to convert Word documents to PDF format.</p>
      {/* Add the actual conversion logic and UI here */}
    </div>
  );
};

export default WordToPdf;