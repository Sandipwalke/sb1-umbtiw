import React from 'react';

const PdfSplitter: React.FC = () => {
  return (
    <div>
      <h1>PDF Splitter</h1>
      <p>This tool allows you to separate PDF pages into individual PDF files.</p>
      {/* Add the actual splitting logic and UI here */}
    </div>
  );
};

export default PdfSplitter;