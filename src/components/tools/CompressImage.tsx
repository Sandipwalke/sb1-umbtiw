import React from 'react';

const CompressImage: React.FC = () => {
  return (
    <div>
      <h1>Compress Image</h1>
      <p>This tool allows you to compress images to reduce file size while maintaining quality.</p>
      {/* Add the actual image compression logic and UI here */}
    </div>
  );
};

export default CompressImage;